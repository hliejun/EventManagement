<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:background="@color/material_blue_grey_800"
             tools:context="sg.edu.nus.comp.orbital.eventmanagement.AddUserContactListFragment">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/add_user_contact_recycler_view"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="0.28" />

    <TextView
        android:id="@+id/empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:visibility="gone"
        android:text="No contacts detected! Your phone address book contacts will be visible here!"
        android:textSize="24sp"
        android:textColor="@color/hint_foreground_material_dark"/>

</FrameLayout>
